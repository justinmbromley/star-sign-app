from setuptools import setup, find_packages

setup(
    name='star-teller-app',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        
    ],
    python_requires='>=3.12',
)
